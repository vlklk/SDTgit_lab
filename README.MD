# 📘 Отчёт по разработке плагина для Visual Studio Code

**Автор:** Лев Волкогонов\
**Группа:** M3124\
**Название плагина:** IncludesCheck

------------------------------------------------------------------------

## 📌 1. Введение

В рамках лабораторной работы был разработан плагин **IncludesCheck** для
Visual Studio Code.\
Его основная задача --- анализировать директивы `#include <...>` в C/C++
файлах и определять:

-   какие заголовочные файлы **используются** (функции/классы из хедера
    применяются в коде),
-   какие заголовочные файлы **не используются**.

После анализа плагин автоматически добавляет в конец файла комментарии
формата:

``` cpp
// <stdio.h> - use
// <stdint.h> - dont use
```

Плагин запускается **только по горячей клавише**, что исключает
автоматические изменения файлов.

------------------------------------------------------------------------

## 📌 2. Архитектура Visual Studio Code и механизм подключения расширений

Visual Studio Code использует модульную архитектуру, построенную на
системе подключаемых расширений.\
Каждое расширение работает в отдельном процессе --- **Extension Host**,
который:

-   загружает экстеншены,
-   управляет их жизненным циклом,
-   предоставляет API для взаимодействия с редактором,
-   обеспечивает безопасную изоляцию.

### Основные компоненты расширения:

### **2.1 package.json**

Описывает плагин, его возможности и настройки:

-   название,
-   список команд,
-   горячие клавиши,
-   условия активации,
-   минимальная версия VS Code.

Пример объявления команды:

``` json
"commands": [
  {
    "command": "includeAnalyzer.run",
    "title": "Analyze Includes"
  }
]
```

Пример горячей клавиши:

``` json
"keybindings": [
  {
    "command": "includeAnalyzer.run",
    "key": "cmd+alt+y",
    "when": "editorTextFocus"
  }
]
```

### **2.2 extension.ts**

Это главный файл, содержащий:

-   регистрацию команды,
-   парсинг include-директив,
-   поиск соответствующих заголовочных файлов,
-   извлечение функций и методов из `.h/.hpp` файлов,
-   анализ фактического использования библиотек,
-   генерацию отчёта в конце файла,
-   вывод предупреждений через Diagnostics API.

### **2.3 Extension Host**

Отдельный процесс, на котором выполняется плагин.\
Запускается автоматически в режиме разработки через F5.

------------------------------------------------------------------------

## 📌 3. Функциональные возможности плагина IncludesCheck

### **3.1 Назначение**

Анализ использования подключённых библиотек C/C++.

### **3.2 Основные функции**

-   Парсинг директив:

    ``` cpp
    #include <...>
    ```

-   Рекурсивный поиск соответствующих `.h/.hpp` файлов внутри workspace.

-   Извлечение:

    -   свободных функций,
    -   методов классов,
    -   методов вида `ClassName::method`,
    -   функций из namespace,
    -   перегруженных операторов.

-   Поиск всех символов по файлу.

-   Определение:

    -   используется ли библиотека (`use`)
    -   или не используется (`dont use`).

-   Автоматическое добавление отчёта в конец файла.

-   Выделение неиспользуемых include предупреждением.

------------------------------------------------------------------------

## 📌 4. Архитектура решения

### **4.1 Сбор include-директив**

Используется регулярное выражение:

``` regex
#include\s*<([^>]+)>
```

### **4.2 Поиск заголовочных файлов**

Рекурсивный обход workspace:

    include/, src/, headers/, libs/, любая вложенная папка

### **4.3 Извлечение функций**

Плагин использует набор регулярных выражений для поиска:

-   методов классов: `ClassName::method`
-   namespace-функций: `ns::func`
-   операторов: `operator<<`
-   обычных функций: `foo()`

### **4.4 Анализ использования**

Плагин ищет каждый найденный символ в тексте `.cpp` файла.

Если найден → библиотека **use**\
Если нет → **dont use**

### **4.5 Генерация отчёта**

Плагин добавляет:

``` cpp
// <vector> - use
// <iostream> - dont use
```

### **4.6 Diagnostics**

Неиспользуемые include подсвечиваются:

-   жёлтой волной (warning)
-   сообщением: "Библиотека `<xxx>`{=html} не используется"

------------------------------------------------------------------------

## 📌 5. Установка и использование плагина

### **5.1 Репозиторий GitHub**

*В этом же репозитории*

### **5.2 Установка**

Открыть проект:

``` sh
code includescheck
```

Установить зависимости:

``` sh
npm install
npm run compile
```

### **5.3 Запуск режима разработки**

``` sh
Fn + F5
```

Откроется окно **Extension Development Host**.

### **5.4 Использование**

Открыть C/C++ файл и нажать:

### `Cmd + Alt + Y`

------------------------------------------------------------------------

## 📌 6. Заключение

В ходе эксперимента был разработан функциональный плагин для Visual
Studio Code, позволяющий анализировать подключённые библиотеки C/C++ и
выводить информацию об их фактическом использовании.

Плагин позволяет:

-   избегать "мусорных" include-ов,
-   оптимизировать кодовую базу,
-   улучшить читаемость и поддержку проекта.

Работа демонстрирует понимание архитектуры IDE, механизма расширений и
практику написания плагинов на TypeScript.
